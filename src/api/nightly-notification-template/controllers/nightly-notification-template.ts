/**
 * nightly-notification-template controller
 */

import { factories } from '@strapi/strapi'

export default factories.createCoreController('api::nightly-notification-template.nightly-notification-template');
