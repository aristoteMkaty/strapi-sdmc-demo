/**
 * nightly-notification-template service
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreService('api::nightly-notification-template.nightly-notification-template');
